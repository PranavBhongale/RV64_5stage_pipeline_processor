## VCD Files

This folder contains the **VCD (Value Change Dump) files** generated by the testbench during simulation.

The VCD files contain waveform information for various test cases. You can open them using a waveform viewer to analyze the behavior of the processor and observe signals across different pipeline stages.

Various test cases can be viewed and analyzed from these waveform files.
